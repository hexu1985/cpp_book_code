#include<iostream.h>
#include<stdlib.h>

int main()
{
   unsigned long x;
   int count;
   x=1;
   count=2;
   cout<<"The value "<<x<<" to rotated left "<<count<<" bits is: "<<_lrotl(x,count)<<endl;
   return 0;
} 
